// array.pop-like function (because IE doesn't support array.pop)
//Array.prototype.popElement = function() {
//	var new_array, i;
//	new_array = new Array();
//	for(i = 0; i < this.length-1; i++) {
//		new_array[i] = this[i];
//	}
//	return new_array;
//}
// array.unshift-like function (because IE doesn't support array.unshift)
//Array.prototype.unshiftElement = function(element) {
//	var new_array, i;
//	new_array = new Array();
//	new_array[0] = element;
//	for(i = 0; i < this.length; i++) {
//		new_array[new_array.length] = this[i];
//	}
//	return new_array;
//}

